<%- include("header")%>
    <% if(msg !="" ){ %>
        <div class="container pt-2">
            <div class="success alert-success" role="success">
                <%= msg %>
            </div>
        </div>
        <% }%>
            <div class="container-fluid">
                <form action="/addnewpassword" method="post">
                    <div
                        class="d-flex flex-row align-items-center justify-content-center justify-content-lg-start pb-3">
                        <p class="lead fw-normal mb-0 me-3">New domain </p>

                    </div>

                    <!-- Email input -->
                    <div class="form-outline mb-4">
                        <input type="text" id="Domain" name="Domain" class="form-control form-control-lg"
                            placeholder="Enter a valid Domain Name" />
                        <label class="form-label" for="Email">Domain</label>
                    </div>

                    <div class="text-center text-lg-start mt-4 pt-2">
                        <button type="submit" class="btn btn-primary btn-lg"
                            style="padding-left: 2.5rem; padding-right: 2.5rem;">submit</button>

                    </div>

                </form>
            </div>
            </body>



            </html>